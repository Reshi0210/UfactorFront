import { PoliticIntegration } from './politic-integration';

describe('PoliticIntegration', () => {
  it('should create an instance', () => {
    expect(new PoliticIntegration()).toBeTruthy();
  });
});
